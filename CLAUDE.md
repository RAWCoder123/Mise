Read `AGENTS.md` in full before writing any code. It is the authoritative
source for conventions, working model, and verification rules in this
repository. This file exists only so Claude Code loads it automatically;
it deliberately duplicates nothing.

Also read `docs/product/mise-operational-backend-master.md` before any task
that touches the operational backend.
