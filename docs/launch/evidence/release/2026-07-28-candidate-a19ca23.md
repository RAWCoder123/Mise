# Provisional beta candidate evidence — `a19ca23`

Candidate commit:
`a19ca2365a559de2c5f14ed28bf38cd289eeceb8`

Verified at: `2026-07-28T23:29:22Z`

This is a provisional invite-only TestFlight beta candidate. Recording these
receipts does not approve a build, production deployment, TestFlight
distribution, restaurant admission, or public release.

## Local release gate

`npm run verify:private-beta-security:local` passed at the exact candidate:

- TypeScript passed.
- 330 application/domain/security tests passed.
- npm audit found zero vulnerabilities.
- Expo Doctor passed all checks.
- Backend security and static design checks passed.
- 566 pgTAP assertions passed after a clean local database rebuild.
- The bounded concurrent workspace-allocation proof passed.
- Local Supabase security advisors reported no issues.
- Production web export completed.
- Every required HTTP route returned 200.
- English, Spanish, and Simplified Chinese interaction QA passed at 390x844
  with zero horizontal overflow.

## Hosted security

`npm run verify:private-beta-security:hosted` passed at the exact candidate
without skipped checks:

- Staging fixtures were created through server-only Auth administration.
- All seven rendered workspace-switch and mutation races passed.
- Hosted tenant workflow authority, quantity bounds, and atomicity passed.
- Service-RPC actor and tenant binding passed.
- Edge concurrency, all-function tenant forgery, role, and rate-limit checks
  passed.
- Append-only operational finding decisions passed.
- Restaurant export returned 25 tenant-scoped datasets, enforced owner/admin
  authority, denied cross-tenant access, bounded counts, and emitted no
  credentials.

## Tenant isolation

The exact candidate passed both database-level and rendered two-tenant proofs:

- All 566 local pgTAP assertions passed.
- Every tested cross-tenant read and mutation remained denied.
- All seven hosted rendered races passed:
  - Today workspace switch
  - Inventory-list workspace switch
  - Inventory-detail workspace switch
  - Insights workspace switch
  - Settings workspace switch
  - Order-detail workspace switch
  - Order-mutation workspace switch
- Stale tenant-A requests never replaced tenant-B state after a workspace
  switch.

## Provider restrictions

`scripts/staging-provider-restrictions-check.mjs` passed at the exact
candidate:

- Square sync and webhooks are off.
- Gmail delivery is off.
- AI insight generation is off.
- Order drafting is off in staging.
- Stripe invoicing is off.
- Ordering policy is `off` for every staging tenant.
- Every tenant has one complete operational-control row.
- The service role cannot use the obsolete unguarded Gmail claim.
- A blocked supplier-delivery attempt created no delivery evidence.
- Supplier communication remains outside Mise for the beta.

## Candidate configuration

- EAS project: `@raymondaws-team/mise`
- EAS project ID: `bf74b605-68fb-4457-9eb8-e68b9c4aac0d`
- iOS bundle identifier: `com.mise.mobile`
- EAS preview environment: no project-scoped variables; cloud build remains
  blocked until bounded staging runtime configuration is added and verified.
- Full Xcode and `simctl`: unavailable; local native verification remains
  pending.

## Receipts intentionally pending

- Managed hosted recovery
- Controlled Sentry receipt
- Controlled PostHog receipt
- Recent supported iPhone
- Older supported iPhone
- Complete critical workflow
- Responsive public privacy/support endpoints and monitored inboxes
- Exact TestFlight build installation
- TestFlight candidate build identity
- Raymond's explicit approval for this exact candidate
