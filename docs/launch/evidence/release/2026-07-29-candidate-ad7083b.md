# Provisional beta candidate evidence — `ad7083b`

Candidate commit:
`ad7083b18af24cd11b66b06bf45145cf1c405237`

Verified at: `2026-07-29T21:05:51Z`

This is a provisional invite-only TestFlight beta candidate. These receipts do
not approve an Apple signing change, cloud build, upload, TestFlight
distribution, restaurant admission, production deployment, or paid action.

## Local release gate

`MISE_MOBILE_LAYOUT_PORT=8104 MISE_MOBILE_LAYOUT_DEBUG_PORT=9354
MISE_MOBILE_LAYOUT_TIMEOUT_MS=240000 npm run
verify:private-beta-security:local` passed at the exact candidate:

- TypeScript and all 333 application, domain, UI-contract, and security tests
  passed.
- `npm audit --audit-level=high` reported zero vulnerabilities.
- Expo Doctor passed 21/21 after the SDK-compatible `expo ~56.0.18` and
  `expo-router ~56.2.17` patch closure.
- Backend static security and design checks passed.
- All 566 pgTAP assertions and the bounded concurrent workspace allocation
  proof passed.
- Local Supabase security advisors reported no issues.
- The production web export and every required route passed.
- English, Spanish, and Simplified Chinese interaction QA passed at 390x844
  with zero horizontal overflow.

## Hosted security

`MISE_STAGING_CLIENT_RACE_PORT=8105
MISE_STAGING_CLIENT_RACE_DEBUG_PORT=9355
MISE_STAGING_CLIENT_RACE_TIMEOUT_MS=180000 npm run
verify:private-beta-security:hosted` passed at the exact candidate without
skipped checks:

- server-only staging fixture provisioning passed;
- all seven rendered workspace-switch and mutation races passed;
- tenant workflow authority, bounds, atomicity, service-RPC binding, Edge
  concurrency, tenant forgery, roles, and rate limits passed;
- append-only operational finding decisions passed; and
- restaurant export returned 25 tenant-scoped datasets with owner/admin
  authority, cross-tenant denial, bounded counts, and secret-free evidence.

## Tenant isolation

The exact candidate passed local database isolation and hosted rendered
two-tenant proofs. Late Luna Bistro reads and mutations did not replace the
active Northside Cafe state across Today, inventory, insights, settings, order
detail, or recommendation approval workflows.

## Provider restrictions

The exact candidate passed
`node --env-file-if-exists=.mise-staging.env
scripts/staging-provider-restrictions-check.mjs`:

- Square sync/webhooks, Gmail delivery, AI insights, order drafting, and
  Stripe invoicing remain off;
- every staging tenant has a complete default-off control row;
- ordering policy remains `off` in staging;
- the unguarded provider claim remains unavailable; and
- a blocked supplier-delivery attempt created no delivery evidence.

The candidate release contract remains `draft_only` with supplier delivery
outside Mise. No autonomous send state exists for this beta.

## Observability boundary

`npm run observability:check` passed the static correlation and whole-event
redaction contract. Live Sentry and PostHog proof credentials are not present
in the trusted local environment, so neither provider receipt is carried
forward from an older candidate. Both remain pending for this exact commit.

## Native prerequisite

The exact candidate passed:

- `npm run qa:ios-prereq`: Xcode 26.6, `simctl`, the iOS 26.5 runtime,
  `com.mise.mobile`, build number 2, launch assets, and the iPhone 17 Pro
  simulator are available.
- `npm run qa:eas-account`: EAS project
  `bf74b605-68fb-4457-9eb8-e68b9c4aac0d` is linked and the authenticated
  `raymondaws-team` account has Owner access.
- `npm run qa:eas-archive`: credentials, local agent state, tests, operations,
  Supabase files, and the independent site remain excluded from the archive.

The machine still has zero valid code-signing identities and zero provisioning
profiles. A paid Apple Developer team, distribution signing, physical devices,
and Raymond's explicit build/distribution approval are still required.

## Receipts intentionally pending

- TestFlight candidate build identity
- Managed hosted recovery on an approved paid recovery target
- Controlled Sentry receipt and alert acknowledgement
- Controlled PostHog receipt
- Recent and older supported physical-iPhone checks
- Complete critical workflow on physical devices
- Public privacy/support access and monitored inboxes
- TestFlight installation
- Raymond's approval for this exact candidate
