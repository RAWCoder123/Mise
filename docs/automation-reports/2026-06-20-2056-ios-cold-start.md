# Mise iOS Demo Readiness Automation Report

Run time: 2026-06-20 20:56 America/New_York

## What Changed

- Made the root startup route session-aware. When the app has a hydrated restaurant, Mise now opens directly to Today instead of sending the operator back to Login.
- Added the same active-session guard to Login so a direct `/login` visit redirects to Today or Setup when a session already exists.
- Updated the shared `Screen` primitive so loading-only states do not need placeholder children.

## Files Touched

- `app/index.tsx`
- `app/(auth)/login.tsx`
- `components/ui/Screen.tsx`
- `docs/automation-reports/2026-06-20-2056-ios-cold-start.md`

## Verification

- `npm run typecheck` passed.
- Expo web server started at `http://localhost:8083`.
- Route smoke checks returned HTTP 200 for `/`, `/login`, `/today`, `/inventory`, `/orders`, `/insights`, `/setup`, and `/settings`.
- Metro bundled successfully with no app runtime errors shown in server output.

## What Still Needs To Be Done

- Complete true mobile-width rendered QA for the core routes on an iPhone-sized viewport.
- Verify the cold-start redirect on an actual iOS simulator or physical device after loading the demo kitchen once.
- Add Expo iOS build metadata and final asset checks before TestFlight-style demo distribution.

## Recommended Next Step

Run an iOS simulator walkthrough: load the demo kitchen, close/reopen the app, confirm it resumes on Today, then sweep Today, Inventory, Orders, Insights, Setup, and Settings for overflow and clipped controls.
