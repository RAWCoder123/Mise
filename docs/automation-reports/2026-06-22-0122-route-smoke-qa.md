# Mise iOS Demo Readiness Automation Report

Run time: 2026-06-22 01:22 America/New_York

## What Changed

- Added `scripts/mobile-route-smoke.mjs`, a repeatable route smoke checker for the iOS demo route set.
- The script can either start Expo web itself or use an existing `MISE_ROUTE_SMOKE_URL`.
- It verifies `/`, `/login`, `/setup`, `/today`, `/inventory`, `/orders`, `/insights`, and `/settings` return a non-empty Expo shell with no obvious framework error-overlay markers.
- Added `npm run qa:routes` and included it in `npm run verify:beta`.
- Updated `docs/ios-demo-checklist.md` and `README.md` so the route smoke command is part of the documented beta/iOS QA path.

## Files Touched

- `scripts/mobile-route-smoke.mjs`
- `package.json`
- `README.md`
- `docs/ios-demo-checklist.md`
- `docs/automation-reports/2026-06-22-0122-route-smoke-qa.md`

## Verification

- `npm run typecheck` passed.
- `npm test` passed: 21 tests.
- `npm run security:static` passed.
- `npm run qa:routes` passed:
  - `/`, `/login`, `/setup`, `/today`, `/inventory`, `/orders`, `/insights`, and `/settings` all returned HTTP 200.
  - Each route returned a shell above the minimum byte threshold.
  - No framework error-overlay markers were detected.

## What Still Needs To Be Done

- This route smoke script does not replace visual iPhone-width inspection. It cannot prove no horizontal overflow or clipped controls.
- Run the app on an iOS simulator or physical iPhone and manually sweep the required routes from `docs/ios-demo-checklist.md`.
- If browser automation becomes available, add screenshot-based mobile viewport checks on top of this shell-level route smoke test.

## Recommended Next Step

Run `npm run ios`, complete the manual iOS simulator route sweep, and fix any safe-area, keyboard, or horizontal-overflow issues found on `/setup`, `/settings`, and the tab screens.
